import DsPlayer from "@/components/ds/DsPlayer";

export default function DsPage() {
    return (
        <main className="min-h-screen bg-(--bg)">
            <div className="mx-auto max-w-7xl">
                <DsPlayer />
            </div>
        </main>
    );
}
